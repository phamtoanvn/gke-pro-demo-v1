apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pro-ingress
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  defaultBackend:
    service:
      name: pro-backend-service
      port:
        number: 80